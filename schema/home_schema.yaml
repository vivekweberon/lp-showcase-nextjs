showcase:
  menu:
    type: string
  properties:
    type: object
  sectionTitle:
    type: string
homePageSectionsOrder:
  type: array

siteOverrides:
  type: object
  additionalProperties:
    type: object
    # Each override object should match the structure under "default"
    
siteName:
  type: array
